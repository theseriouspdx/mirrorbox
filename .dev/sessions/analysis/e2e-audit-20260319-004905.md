# E2E Audit Notes — 2026-03-19 00:49:05

Transcript reviewed:
- `/Users/johnserious/MBO_Alpha/.mbo/logs/mbo-e2e-20260319-002908.log`

## Resume Context

- Worktree validator passes.
- `src/auth/operator.js` contains an in-progress NL-first reasoning patch.
- Remaining gap is contract-level: reasoning roles still flow through JSON-oriented validation/recovery paths and still emit transcript-hostile warning strings.

## Confirmed Runtime Violations

- Reasoning roles are still treated as JSON-first in `src/auth/call-model.js` schema validation, which can emit `returned non-JSON` and `produced malformed JSON` warnings even when prose output is acceptable.
- `src/auth/operator.js` still contains planner/reviewer fallback logging keyed to malformed output language, including:
  - `Planner output could not be reconciled into a stable NL plan. Falling back to minimal safe plan.`
  - `Reviewer produced malformed output. Falling back to planner draft for comparison.`
- The current extractor is section-based but still too narrow for Markdown variance and does not yet fully enforce the reasoning-role contract at every stage.

## Required Patch Direction

- Treat `architecturePlanner`, `componentPlanner`, `reviewer`, and `tiebreaker` as reasoning roles whose primary contract is labeled prose sections, not JSON.
- Keep strict JSON validation only for machine-required roles and transports.
- Make section extraction resilient to minor Markdown variance such as `## RATIONALE`, `### RATIONALE`, or bolded headers.
- Avoid transcript-hostile warnings when required prose sections are present.
- Preserve readable prose in the audit path so DID artifacts remain human-auditable in Alpha logs.

## Resume Plan

1. Patch `src/auth/call-model.js` so reasoning-role schema validation no longer emits non-JSON or malformed-JSON warnings.
2. Patch `src/auth/operator.js` to use labeled-section extraction as the first-class parsing path for reasoning roles and replace fallback warning strings with contract-accurate prose.
3. Sync the updated runtime file(s) into `/Users/johnserious/MBO_Alpha` only after preserving the existing Alpha transcripts.
4. Run a fresh dedicated Alpha E2E transcript and verify the log does not contain:
   - `returned non-JSON`
   - `produced malformed JSON`
   - `Planner returned malformed output`
   - `fallback to minimal safe plan`

## DID / Onboarding Follow-On

- After the runtime contract is clean in Alpha, prepare:
  - DID duplication plan for the governance-sensitive follow-up.
  - Final pristine setup/onboarding rerun plan with dedicated transcript capture.
