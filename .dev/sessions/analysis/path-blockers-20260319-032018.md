# Path Blockers — Installer-First Alpha Run (2026-03-19)

## Scope
- Controller source: `/Users/johnserious/MBO`
- Target runtime: `/Users/johnserious/MBO_Alpha`
- Entry contract used: `npx mbo` from target runtime

## Blocking Findings (Path/Root Only)

1. **Controller root metadata points at Alpha, so Alpha is treated as controller path**
- Observed file: `~/.mbo/config.json`
- Current values:
  - `controllerRoot: /Users/johnserious/MBO_Alpha`
  - `installRoot: /Users/johnserious/MBO_Alpha`
- Result: self-run warning appears in Alpha even though warning is intended only for controller repo path.

2. **Persistent warning trigger is path-driven from home config, not package install source**
- Logic path: `bin/mbo.js -> setSelfRunWarningEnv()`
- Decision source: `~/.mbo/config.json controllerRoot`
- Result: any cwd under configured controllerRoot gets warning banner regardless of whether that cwd is actual controller repo.

3. **Non-TTY onboarding gate loops after root-mismatch re-onboarding requirement**
- Runtime output observed:
  - `[SYSTEM] Project root mismatch detected (stored: unknown — profile predates root-tracking). Triggering re-onboarding.`
  - `ERROR: Non-TTY launch requires onboarding but no onboarding.json found.`
  - `[MBO] Operator exited with code 1. Respawning...`
- Path relevance:
  - onboarding profile under `/Users/johnserious/MBO_Alpha/.mbo/onboarding.json` has stale/missing `projectRoot` semantics,
  - so path mismatch forces onboarding,
  - non-TTY path then exits and respawn loop begins.

4. **Packaged installer currently includes controller/worktree/governance trees**
- Command evidence: `scripts/mbo-install-alpha.sh` (`npm pack`) tarball listing includes `.dev/`, `.dev/worktree/`, backups.
- Path impact: runtime artifact carries controller-side paths and stale artifacts into Alpha, increasing path identity drift and config contamination risk.

## Supporting Snapshot
- `~/.mbo/config.json` currently:
  - `controllerRoot=/Users/johnserious/MBO_Alpha`
  - `installRoot=/Users/johnserious/MBO_Alpha`
- Alpha `.mbo` exists with onboarding/config/session files, but root mismatch still triggers re-onboarding due to profile root-tracking expectations.

## Session Outcome
- No code patch applied in this step (per instruction).
- Logged blockers only.
