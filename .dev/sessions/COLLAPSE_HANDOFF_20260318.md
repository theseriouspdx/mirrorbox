# Governance Collapse — Session Handoff
## 2026-03-18 | Status: COLLAPSE-01 DRAFTED, AWAITING APPROVAL

---

## What Happened This Session

This was a **read-only analysis + drafting session**. Two agents worked in sequence:
1. A DID analysis agent produced `ANALYSIS_HANDOFF_20260318.md` (full audit + conflict resolutions)
2. Claude (this session) read that file, confirmed the conflict resolutions, and drafted COLLAPSE-01

**No source code was written. No governance files were modified except projecttracking.md.**

The one projecttracking.md change made this session:
- Added `COLLAPSE-05` (invariant renumbering task) to Backlog — see below

---

## The Big Picture — Why This Exists

MBO is pivoting from a process-based trust model to an **API-native payload-based trust model**.
Agents are stateless text generators. They cannot touch the filesystem. The only dangerous
moment is the write step. Therefore the entire verification stack collapses to one hard gate
at the write step.

The governance document structure collapses from its current sprawl into THREE documents:
- `.dev/governance/AGENTS.md` — roles and permissions (hand-authored, dev-only)
- `.mbo/STATE.md` — current project state (generated by session-close)
- `.dev/spec/SPEC.md` — implementation contract

---

## What COLLAPSE-01 Through COLLAPSE-05 Mean

| Task | What It Is | Classification |
|------|-----------|----------------|
| COLLAPSE-01 | Collapse `.dev/governance/AGENTS.md` — merge both source versions, apply all C1-C6 decisions | REQUIRED |
| COLLAPSE-02 | Create `.mbo/STATE.md` format + update `session-close.sh` to write it | REQUIRED |
| COLLAPSE-03 | `.mbo/` footprint migration; generated client `AGENTS.md` from onboarding profile | DEFERRED |
| COLLAPSE-04 | `NEXT_SESSION.md` path consolidation to `.mbo/sessions/` only | REQUIRED — ships with COLLAPSE-02 |
| COLLAPSE-05 | Renumber AGENTS.md invariants to eliminate gaps | BACKLOG after COLLAPSE-01 |

Required execution order:
1. Approve COLLAPSE-01 draft (below) → write `.dev/governance/AGENTS.md`
2. Draft + approve COLLAPSE-02+04 together → update session-close.sh
3. Update AGENTS.md Section 1 read paths to match new locations
4. Validate Gate 0 end-to-end before closing

---

## The Six Conflict Resolutions (Final — No Further Debate)

These decisions are locked. Do not re-open them.

- **C1 — Invariant 17:** Worktree-Isolated is canonical. Full `git worktree add` protocol.
  "Branch-isolated" language from root AGENTS.md is superseded and must not appear.

- **C2 — Invariant 18:** DB Write Prohibition (from .dev/governance/AGENTS.md).
  Old Invariants 18/19/20 (MCP endpoint resolution, success semantics, graph backoff)
  are REMOVED. They compensated for process complexity that no longer exists under
  API-native model. Any residual MCP behavioral guidance belongs in docs/, not invariants.

- **C3 — MCP health check:** `node scripts/mcp_query.js --diagnose graph_server_info`
  everywhere. All curl-only health check language removed from Section 11.

- **C4 — DDR-001/002/003:** Each gets a task entry in projecttracking.md AND normative
  content promoted into SPEC.md as a proper numbered section (Sections 40/41/42).
  STATE.md tracks status only — it does not own design decisions.

- **C5 — TECHNICAL_DEBT.md:** Two items migrated to BUGS.md as open verification tasks,
  then TECHNICAL_DEBT.md moves to `.dev/archive/`. Keeps collapse to three governing docs.

- **C6 — NEXT_SESSION.md canonical path:** `.mbo/sessions/NEXT_SESSION.md` only.
  session-close.sh writes there only. Short compatibility window: symlink old paths
  during transition, then remove.

---

## COLLAPSE-01 Draft — Full Text

This is the approved draft of `.dev/governance/AGENTS.md` collapsed source.
**Human operator has reviewed and approved the structure. Next agent writes this file.**


---

```markdown
# AGENTS.md
## Mirror Box Orchestrator — Autonomous Agent Governance (v1.0)

**Scope:** This document governs MBO's own development pipeline. It is read at Gate 0
of every development session. It is NOT deployed to client projects — client projects
receive a generated `.mbo/AGENTS.md` produced by `mbo setup` from the onboarding profile.

---

## Section 1 — Session Start Protocol

Every development session begins in this order. No exceptions.

1. Read this file (`AGENTS.md`)
2. Read `.dev/governance/projecttracking.md` — identify current milestone and active task
3. Read `.dev/governance/BUGS.md` — check linked P0/P1 blockers for the active task
4. Read `.mbo/sessions/NEXT_SESSION.md` — generated handoff only (do not hand-edit)

No code is written before this sequence completes.

---

## Section 2 — Development Protocol (DID) & Routing Tiers

**Every task must exist in `projecttracking.md` before code is written.**

The agents are interchangeable peers. Before executing a task, the human Operator
determines the Routing Tier:

**Tier 0 & 1 (Low Risk / Single File / Docs):**
- A single agent proposes the implementation and writes the diff.
- If the human approves, that agent implements it directly.

**Tier 2 & 3 (High Risk / Multi-File / Architecture / Governance):** (Dual Independent Derivation)
1. Any modification to `SPEC.md`, `AGENTS.md`, or the governance directory.
2. **Agent A (Gemini)** proposes the implementation and writes the diff.
3. **Agent B (Claude)** MUST independently derive their own implementation BLIND —
   no Agent A output visible.
4. The human Operator reconciles the two versions.
5. Only after reconciliation and human "go" is the implementation committed.
6. If unresolvable: Claude gets one retry on the conflict zone only. If still
   unresolved: **Codex** is the tiebreaker.

Full protocol: `.dev/governance/DID-PROTOCOL.pdf`
Implementation task: `1.1-H24` — see `.dev/preflight/did-protocol-implementation.md`

---

## Section 3 — Authorship & Attribution Policy

1. **Authorship is for the Architect:** No agent shall claim authorship credit on
   commit pushes, pull requests, or documentation.
2. **No "AI-Generated" Branding:** Agents must not prepend "AI-Generated" or similar
   self-crediting labels to commit messages unless explicitly configured by the
   architect for audit purposes.

---

## Section 4 — What Agents Can and Cannot Do (Security Invariants)

| Agent | Can | Cannot |
|-------|-----|--------|
| Gemini CLI | Peer developer. Propose, review, derive, and implement. | Write to src/ without task in projecttracking.md |
| Claude Code | Peer developer. Propose, review, derive, and implement. | See the other agent's reasoning before producing its own |

### Core Security Invariants (v1.0)

1. **Invariant 1:** No unapproved file modifications.
2. **Invariant 2:** No write access outside project root.
3. **Invariant 3:** No execution without human approval.
4. **Invariant 4:** Every operation reproducible from the event store.
5. **Invariant 5:** Failed builds revert automatically.
6. **Invariant 6:** No model bypasses `callModel`.
7. **Invariant 7:** Context isolation enforced at runtime.
8. **Invariant 8:** Secrets never enter sandbox execution.
9. **Invariant 9:** Local models stay local.
14. **Invariant 14:** No `write_file` for overwrites. `replace` MUST be used for all
    modifications to existing files. `write_file` is reserved for new files only.
18. **Invariant 18 (DB Write Prohibition):** Agents MUST NOT write directly to any
    database (mirrorbox.db, dev-graph.db, or any SQLite file) under any circumstances.
    The `tasks` table and all pipeline state is seeded exclusively by the Operator
    running the pipeline from `projecttracking.md`. Agents interact with state only
    through governance files (`projecttracking.md`, `BUGS.md`). Direct `INSERT`,
    `UPDATE`, or `DELETE` against the DB — via node eval, sqlite3 CLI, or any MCP
    tool call that proxies raw SQL — is a governance violation equivalent to bypassing
    the handshake protocol.
```

```markdown
---

## Section 5 — Session Management & Two-World Integrity

### Invariant 10 (Two-World Isolation)
- Every operation shall explicitly declare its target scope: `world_id = mirror | subject`.
- Reads may span worlds; writes must target exactly one world.
- Missing or invalid `world_id` is a hard failure.

### Invariant 11 (Graph Investigation)
- Retrieval must use `graph_search` and `graph_query_impact`.
- Full SPEC/file-dump context injection is disallowed by default.

### Invariant 12 (HardState Budget)
- Enforce 5,000-token HardState ceiling.
- Allocation: 600 invariants, 1,800 evidence, 1,200 prior decisions, 900 reasoning,
  500 reserve.

### Invariant 13 (Pre-Mutation Checkpoints & Backups)
- Immutable checkpoint required before any mutation in either world.
- **Atomic Topology Backup:** Before any file modification or creation, a backup of
  the original document MUST be written to `.dev/bak/` preserving the same directory
  topology as the repository.

### Invariant 15 (Non-Destructive Recovery)
- Prohibit `rm -f` and delete/recreate schema recovery patterns.

### Invariant 16 (Atomic Session-End Backup)
- On session close, atomically persist event flush, graph delta, handoff record, and
  integrity hashes.
- Run `PRAGMA integrity_check`.

### Invariant 17 (Worktree-Isolated Verification Gate)
- All implementation work MUST occur in a dedicated git worktree with its own branch
  (`<agent>/<task-or-scope>`, e.g. `claude/`, `gemini/`, `codex/`).
- Worktree path convention: `.dev/worktree/<agent>-<task>/`
- Create with: `git worktree add .dev/worktree/<agent>-<task> -b <agent>/<task>`
- Each agent session MUST use its own worktree. Sharing a working directory between
  concurrent agents is prohibited.
- No apply/merge/promotion to the target branch until verification gate passes.
- Verification gate minimum: `python3 bin/validator.py --all` passes, task-specific
  tests pass, and no new P0/P1 regressions are introduced.
- If verification fails, fix-forward on the same working branch; do not apply partial
  changes.
- On session close: `git worktree remove .dev/worktree/<agent>-<task>` (after
  branch finalization).

---

## Section 6 — Proactive State Sync & Session Protocols

### 6A. The Audit Gate (Completion Trigger)
Halt and present: unified diff, `python3 bin/validator.py --all` output, one-paragraph
summary.

### 6B. State Sync (Automatic on Approval)
On `approved`: Update `projecttracking.md`, `CHANGELOG.md`, `BUGS.md`, and commit.

### 6C. Session End
Write `.mbo/sessions/NEXT_SESSION.md`, terminate MCP, run Branch Finalization, output
checklist.

### 6D. Branch Finalization (Required Before Session Close)
1. Promote approved, verified branch to `main` via approved merge strategy.
2. Record promotion outcome in session artifacts.
3. Remove working branch after successful promotion (local + remote) unless retained.
4. Retained branches: rename under `archive/` with short reason in session handoff.

---

## Section 7 — Critical Constraints

1. `callModel` is the only path to any model.
2. No file write outside approved scope.
3. No code touches a real repository without human `go`.
4. Every model call logged to event store before and after.
5. Reviewer never sees planner output before Stage 4C.

---

## Section 8 — Required Affirmation

Before this session proceeds, read governance files and report base nhash:
`(AGENTS sections + BUGS sections + NEXT_SESSION sections) × 3 = [Base Result]`

Wait for human salt. Calculate `nhash * salt = Hard State Anchor`.

**Non-Persistence Mandate:** MUST NOT document, persist, or commit the nhash, salt, or
Anchor in any file. Exists only in `[Hard State: X]` internal thinking blocks.

Report Anchor and state: "I will not make assumptions about the intentions of the human
at any time."

Identify next scheduled task from `projecttracking.md` and ask: "Would you like to work
on this or something else?"

---

## Section 9 — Strict Mode (Code Output Constraints)

1. **Unified Diffs Only.**
2. **No stylistic or cleanup changes** unless fixing a functional bug or >10% perf gain.
3. **Handshake Protocol:**
   - `src/**`: Locked (444). Ask human for `mbo auth src`, wait for `go`, use
     `impl_step.sh`.
   - `.dev/governance/**`, `scripts/**`, `bin/**`: Unlocked. Use `impl_step.sh`.

---

## Section 10 — Hard State Persistence

State **Hard State Anchor** at beginning of every `<thinking>` block:
`<thinking>[Hard State: X] ...`

**No File Modifications Without Explicit Approval:** Agents must NOT edit, create, or
modify any file without explicit human authorization ("go"). All file interactions must
use MCP tools.

---

## Section 11 — Graph-Assisted Context

Run only the queries listed in NEXT_SESSION.md. Load only the files those queries return.

**MCP connection:**
Graph server is a launchd system daemon at project-scoped dynamic endpoint.
No fixed port, manifest-based discovery required.

If graph server unavailable:
- Check: `node scripts/mcp_query.js --diagnose graph_server_info`
- Start: `mbo setup`
- State: "Dev graph unavailable. Ran `node scripts/mcp_query.js --diagnose
  graph_server_info` — [result]. Awaiting human instruction."

Verify before starting: `node scripts/mcp_query.js --diagnose graph_server_info`
```

```markdown
---

## Section 12 — MBO Operational Workflow (Sovereign Loop)

Every development session MUST follow this checklist:
1. **Identify Task** from `projecttracking.md`.
2. **Permission Check** via `python3 bin/handshake.py <cell_name>`.
3. **Create Working Worktree** — `git worktree add .dev/worktree/<agent>-<task>
   -b <agent>/<task>` — work exclusively inside it. Never share with concurrent session.
   > **DB Write Prohibition:** No agent may write directly to mirrorbox.db or any
   > pipeline database at any point. All task state flows through governance files →
   > Operator → pipeline. (See Invariant 18.)
4. **Derive & Propose** solution from graph context.
5. **Human Approval** ("go").
6. **Implement on Branch & Verify** (`python3 bin/validator.py --all` + task tests).
7. **Verification Gate Decision** — promotion allowed only after required checks pass.
8. **Audit Gate** (diff + validator output + summary).
9. **Sync State** (`projecttracking.md`, `CHANGELOG.md`, `BUGS.md`).
10. **Branch Finalization** (promote, then delete or archive branch).
11. **Lock & Journal** (`handshake.py --revoke` and `init_state.py`).

---

## Section 13 — Persona Store & Reasoning Constraints

### 13A. Persona Resolution
1. `~/.orchestrator/personas/<id>.md` (User library)
2. `<project>/.mbo/personalities/<id>.md` (Project overrides)
3. `<MBO_ROOT>/personalities/<alias>.md` (System defaults)

### 13B. Reasoning Constraints
- **Classifier (The Sentry):** "When in doubt, escalate."
- **Planner (The Minimalist):** Every line must earn its place.
- **Reviewer (The Skeptic):** Derive first, then compare.
- **Tiebreaker (The Arbitrator):** Select, don't synthesize.
- **Operator (The Anchor):** Human-readable status only.

### 13C. Project Assignments (`.mbo/persona.json`)
Projects define active personas in `.mbo/persona.json`. Travels with the repository.

---

## Section 14 — Entropy Gate & Decomposition

### 14A. Entropy Score
- Critical Assumption: 3.0 pts | High Impact: 1.5 pts | Low Impact: 0.5 pts

### 14B. Hard Stop
- **Entropy > 10:** BLOCKED. Pipeline halts before planning begins.
- **Remedy:** Decompose into subtasks until each entropy ≤ 10.

---

## Section 15 — Task Versioning Scheme

**Legacy (pre-2026-03-16):** `<Milestone>-[Type]<TaskNum>` e.g. `1.1-H37`
**New (from 2026-03-16):** `v<Major>.<MilestoneNN>.<TaskNum>` e.g. `v0.11.37`
- Major: `0` | MilestoneNN: dot removed (1.1 → `11`) | TaskNum: zero-padded
- Completed tasks: retain legacy IDs. Outstanding tasks: carry both IDs.
- New tasks from 2026-03-16: new format only.

---

## Section 16 — Workflow Canonicalization (Single Source of Truth)

1. `projecttracking.md` is the only canonical task ledger.
2. Every active task MUST include: Task ID, Type, Status, Owner, Branch, Updated,
   Links, Acceptance.
3. `BUGS.md` is bug registry only. Every OPEN/PARTIAL bug MUST link to a task ID.
4. `NEXT_SESSION.md` is generated output only. Manual edits are prohibited.
   Canonical path: `.mbo/sessions/NEXT_SESSION.md`
5. Session-close regenerates `.mbo/sessions/NEXT_SESSION.md` from projecttracking.md
   + OPEN/PARTIAL P0/P1 bugs.
6. `python3 bin/validator.py --all` MUST fail when: any P0/P1 bug lacks linked task,
   linked task doesn't exist in projecttracking, or NEXT_SESSION.md is stale.

Authoring rule: update state only at task status transitions and at session close.

---

## Section 17 — Deployment Footprint & Generated Governance

### 17A. .mbo/ Footprint Contract
All MBO-authored files in client projects live under `.mbo/`. Nothing MBO-authored at
the project root. The project root belongs to the project owner.

Generated on `mbo setup`:
- `.mbo/AGENTS.md` — project-scoped governance (generated, NOT this document)
- `.mbo/STATE.md` — agent-facing state (generated by session-close)
- `.mbo/sessions/NEXT_SESSION.md` — session handoff (generated by session-close)

Runtime: `.mbo/mirrorbox.db`, `.mbo/config.json`, `.mbo/onboarding.json`,
`.mbo/personalities/`, `.mbo/logs/`, `.mbo/run/`, `.mbo/sessions/`

### 17B. Generated Client AGENTS.md Contract
`.mbo/AGENTS.md` is produced by `mbo setup` from the onboarding profile. Contains:
project name, prime directive, authorized agents, danger zones, verification commands,
write scope policy, pointer to MBO dev docs. NOT a copy of this document. Regenerated
on re-onboard. Versioned in mirrorbox.db.

---

*Last updated: 2026-03-18 — Governance collapse (COLLAPSE-01). C1–C6 resolved.
Invariant 17 → Worktree-Isolated. Invariant 18 → DB Write Prohibition. Invariants 19/20
removed (superseded by API-native model). Section 11 → mcp_query.js. Section 17 added.
NEXT_SESSION.md canonical path → .mbo/sessions/NEXT_SESSION.md.*
```

---

## Known Issues To NOT Ignore

### Invariant Numbering Gap (COLLAPSE-05)
The invariant list has gaps: 1-9, then 14, then 18. Numbers 10-13, 15-17 are used as
named invariants in Section 5, not in the Section 4 numbered list. This is inherited
from both source files and is intentional-but-confusing. **Do not fix it in this pass.**
COLLAPSE-05 is in projecttracking.md Backlog.

**Before COLLAPSE-05 is executed, grep these files for literal invariant number strings:**
- `docs/agent-onboarding.md` — references "Invariant 14"
- `src/templates/AGENTS.md` — references Invariants 13, 15, 16 (this is the generated
  client template — needs its own review; do NOT conflate with `.dev/governance/AGENTS.md`)
- `CHANGELOG.md` — historical references to Invariant 14 (leave untouched — immutable history)

### src/templates/AGENTS.md — Not Yet Reviewed
This file is the source template for the generated `.mbo/AGENTS.md` that ships to client
projects. It has its own invariant structure (Inv 13/15/16 visible). It was NOT part of
this collapse analysis. COLLAPSE-03 ("generated client AGENTS.md from onboarding profile")
will need to address this template. Whoever picks up COLLAPSE-03: read this file first.

---

## What The Next Agent Should Do

1. **If approving COLLAPSE-01:** Write the draft above to
   `/Users/johnserious/mbo/.dev/governance/AGENTS.md`.
   This is a Tier 2 governance change — DID protocol applies. Gemini should derive
   independently before Claude writes.

2. **After COLLAPSE-01 is written and committed:** Move to COLLAPSE-02+04 together.
   Draft STATE.md format and the session-close.sh changes. Those are the Gate 0 fixes
   that unblock every subsequent session.

3. **Do not start COLLAPSE-03 or COLLAPSE-05** until COLLAPSE-01 + COLLAPSE-02+04 are
   committed and Gate 0 validates end-to-end.

---

## Source Files Read This Session

- `/Users/johnserious/mbo/.dev/sessions/ANALYSIS_HANDOFF_20260318.md` (479 lines — full audit)
- `/Users/johnserious/mbo/.dev/governance/AGENTS.md` (264 lines — .dev source)
- `/Users/johnserious/mbo/AGENTS.md` (296 lines — root version, superseded as source)
- `/Users/johnserious/mbo/.dev/governance/projecttracking.md` (modified: COLLAPSE-05 added)

Cross-reference search run: `Invariant 1[4-9]|Invariant 20|DB Write Prohibition|See Invariant`
across all .md, .ts, .js, .py, .sh files. Results documented above.

---

*Written: 2026-03-18 — Claude session, no source code writes*
*projecttracking.md change: COLLAPSE-05 added to Backlog*
*Next session: approve COLLAPSE-01 draft, execute write, then COLLAPSE-02+04*
